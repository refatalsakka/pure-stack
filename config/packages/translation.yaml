framework:
    default_locale: '%env(DEFAULT_LANGUAGE)%'
    translator:
        default_path: '%kernel.project_dir%/translations'
        fallbacks:
            - '%env(DEFAULT_LANGUAGE)%'
        providers:
